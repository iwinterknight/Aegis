[pytest]
# The Aegis package lives under src/, so put it on the import path for tests
# (lets `from aegis.ingestion import ClaimRecord` resolve without installing).
pythonpath = src
testpaths = tests
addopts = --import-mode=importlib